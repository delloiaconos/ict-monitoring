global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Choose ONE (or more) notification method(s):
    # email_configs:
    #   - to: you@example.com
    #     from: alertmanager@example.com
    #     smarthost: smtp.example.com:587
    #     auth_username: alertmanager@example.com
    #     auth_password: "REPLACE_ME"
    #
    # slack_configs:
    #   - api_url: "REPLACE_ME"
    #     channel: "#alerts"
    #
    # webhook_configs:
    #   - url: "http://your-webhook:port/path"
